import styled from 'styled-components';

import { Box, BoxProps } from '..';

/* <Flex />
============================================================================= */
const Flex = styled(Box)<BoxProps>({ display: 'flex' });

export default Flex;
